# Check RAID state

define service {
    service_description     RAID - $VALUE$
    use                     generic-service
    register                0
    host_name               linux-raid-by-ssh
    check_command           check_raid_by_ssh!$VALUE$
    duplicate_foreach       _raid_plugins
    aggregation             raid
}
